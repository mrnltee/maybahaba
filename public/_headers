# Cloudflare static asset headers.
#
# Next.js fingerprints everything under /_next/static/, so those files are
# immutable and safe to cache forever. Nothing else is cached aggressively
# — flood data is time-sensitive and must not be served stale.

/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
