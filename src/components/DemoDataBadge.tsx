export function DemoDataBadge() {
  return (
    <span
      className="inline-flex items-center rounded-full border border-(--color-border) bg-(--color-surface) px-2 py-0.5 text-[11px] font-semibold uppercase tracking-wide text-(--color-ink-faint)"
      title="Sample data for local development — not a real community report."
    >
      Demo data
    </span>
  );
}
